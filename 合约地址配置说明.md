# 合约地址配置说明

## 📋 已部署的合约地址（Sepolia 测试网）

所有合约已成功部署到 Sepolia 测试网并已验证：

| 合约名称 | 地址 | Etherscan |
|---------|------|-----------|
| **EventManagement** | `0xCA58572abaA32748384641440DEAbDABc1b1831D` | [查看](https://sepolia.etherscan.io/address/0xCA58572abaA32748384641440DEAbDABc1b1831D) |
| **RegistrationSBT** | `0x44dEB2A6d138B148b27388122EDF96fEFc241267` | [查看](https://sepolia.etherscan.io/address/0x44dEB2A6d138B148b27388122EDF96fEFc241267) |
| **CheckIn** | `0x2f43D00157E70CaE0b1aBcEC66e42B57E53C642B` | [查看](https://sepolia.etherscan.io/address/0x2f43D00157E70CaE0b1aBcEC66e42B57E53C642B) |
| **SubmissionRegistry** | `0x37FAc80dc206b97098Ae464D00B2056D5418eb93` | [查看](https://sepolia.etherscan.io/address/0x37FAc80dc206b97098Ae464D00B2056D5418eb93) |

## ✅ 已完成的配置

### 前端配置
- ✅ 已创建 `frontend/.env` 文件，包含所有合约地址
- ✅ 已更新 `frontend/src/utils/contractUtils.js`，支持多合约地址配置
- ✅ 合约地址已硬编码为默认值（即使环境变量未设置也能工作）

### 后端配置
- ✅ 后端不需要硬编码合约地址
- ✅ 合约地址会存储在数据库中（Event 表的 `contract_address` 字段）
- ✅ 每个活动可以关联不同的 PrizePool 合约地址

## 🔧 如何使用

### 前端使用

合约地址已配置在 `frontend/.env` 文件中，前端会自动读取：

```javascript
import { CONTRACT_ADDRESSES } from './utils/contractUtils'

// 获取 Sepolia 网络的 EventManagement 合约地址
const eventManagementAddress = CONTRACT_ADDRESSES.sepolia.eventManagement

// 获取其他合约地址
const registrationSBTAddress = CONTRACT_ADDRESSES.sepolia.registrationSBT
const checkInAddress = CONTRACT_ADDRESSES.sepolia.checkIn
const submissionRegistryAddress = CONTRACT_ADDRESSES.sepolia.submissionRegistry
```

### 后端使用

后端不需要配置合约地址，因为：
1. 创建活动时，前端会调用链上合约并返回合约地址
2. 合约地址会保存到数据库的 `events.contract_address` 字段
3. 后续操作可以通过数据库查询获取合约地址

## 📝 注意事项

1. **环境变量优先级**：
   - 如果设置了环境变量，会优先使用环境变量
   - 如果没有设置，会使用代码中的默认值（已部署的地址）

2. **.env 文件安全**：
   - `.env` 文件已添加到 `.gitignore`，不会被提交到 Git
   - 不要将包含私钥的 `.env` 文件提交到代码仓库

3. **多网络支持**：
   - 当前只配置了 Sepolia 测试网
   - 如需部署到其他网络，在 `.env` 文件中添加对应的环境变量

4. **PrizePool 合约**：
   - PrizePool 是按活动部署的，每个活动有独立的 PrizePool
   - PrizePool 地址会存储在 `funding_pools.contract_address` 字段中

## 🔗 相关链接

- Sepolia 测试网浏览器: https://sepolia.etherscan.io/
- Sourcify 验证: https://repo.sourcify.dev/
- 合约源码: `contract/contracts/` 目录

## 🎯 下一步

现在你可以：
1. ✅ 启动前端和后端服务
2. ✅ 创建活动（会自动调用 EventManagement 合约）
3. ✅ 测试注册、签到、提交等功能
4. ✅ 根据需要调用其他合约（RegistrationSBT, CheckIn, SubmissionRegistry）

所有合约地址已配置完成，可以开始使用了！🎉

