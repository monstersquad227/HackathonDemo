### 创建新活动页面 ✅ 已完成
+ ✅ 链上创建（需要连接钱包）改为基本信息字段
+ ✅ 链上创建按钮默认是勾选的
+ ✅ 当勾选链上创建时，需要选定网络，默认为Sepolia
+ ✅ 根据实际情况提供其他EVM选项，包括测试网络
+ ✅ 当勾选链上创建时，会同时会将活动信息上链
+ ✅ 同时优化智能合约侧的代码

#### 实现说明
1. **前端改动**：
   - 创建了 `frontend/src/utils/contractUtils.js` 工具文件，包含网络配置和合约交互功能
   - 修改了 `EventCreate.jsx` 组件：
     - 将"链上创建"选项移到基本信息部分
     - 默认勾选链上创建
     - 添加网络选择器（支持Sepolia、Goerli、Mumbai、Polygon、Ethereum、BSC等）
     - 实现链上创建逻辑，在提交时先调用智能合约，再调用后端API

2. **智能合约优化**：
   - 添加了自定义错误（更省gas）
   - 添加了字符串长度限制（最大500字符）
   - 添加了时间序列验证
   - 添加了奖项数量限制（最多50个）
   - 改进了错误处理机制

3. **使用说明**：
   - 需要在环境变量中配置合约地址（如 `VITE_EVENT_MANAGEMENT_CONTRACT_SEPOLIA`）
   - 用户需要先连接钱包才能进行链上创建
   - 系统会自动切换到选定的网络